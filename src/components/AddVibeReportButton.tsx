
import React from 'react';
import { Navigate } from 'react-router-dom';

const AddVibeReportButton = () => {
  // This component is now replaced by ActionMenu in Navbar
  return null;
};

export default AddVibeReportButton;
